
import React from 'react'
// import streams from "../streams.json";

const Card = ({children}) => {
  return (
    <div className='flex items-center flex-col'>
      
    {children}</div>
  )
}

export default Card
