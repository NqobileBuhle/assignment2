import Hero from "../components/Hero";
import NotFound from "../components/NotFound";

const NotFound = () => {
  return (
    <div>
      <Hero />
      <NotFound />
    </div>
  );
};

export default NotFound;